<h1><%= @cocktail.name %></h1>
<ul>
    <% @cocktail.doses.each do |dose| %>
        <li><%= dose.ingredient.name %> - <%= dose.description %><%= link_to 'X', dose_url(dose), method: 'delete' %></li>
    <% end %>
</ul>

<%= link_to 'Add dose', new_cocktail_dose_url(@cocktail) %>

<%= link_to 'Go back', cocktails_url %>

